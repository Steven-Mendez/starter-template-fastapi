## Why

`pyproject.toml:135` sets `select = ["E", "F", "I", "B"]`. The codebase already has `# noqa: S, DTZ003, PLC0415, BLE001, ARG001/2` comments — proving contributors expect tighter rules. No `per-file-ignores` block means tests would trip `S101` (`assert`) if rules were widened naively.

## What Changes

- Expand `[tool.ruff.lint] select` to: `["E","F","I","B","UP","S","C4","RUF","SIM","PT","TRY","RET","ARG","DTZ","ASYNC","PERF","PL"]`.
- Add `[tool.ruff.lint.per-file-ignores]`:
  - `"**/tests/**"` and `"**/conftest.py"`: `["S101","S105","S106","ARG","PLR2004"]`.
  - `"src/features/**/adapters/outbound/persistence/sqlmodel/models.py"`: `["RUF012"]` (SQLModel idiomatic mutable class defaults).
  - `"alembic/versions/*.py"`: `["ALL"]` (autogenerated).
- Project-wide `extend-ignore`: `["TRY003","PLC0415"]` (rationale documented in `design.md`).
- Run `make lint-fix` for auto-fixable findings (`UP`, `SIM`, `C4`, `RET`); hand-fix the rest (mostly `S`).

**Capabilities — Modified**: `quality-automation`.

## Impact

- **Code**: `pyproject.toml`, plus cleanups across `src/` for newly-enabled rules.
- **CI**: tighter `make lint` gate.
- **Backwards compatibility**: none — CI-gate change only.
