# refactor-to-feature-first

Reorganize codebase to feature-first layout with src/platform/ shared kernel and src/features/kanban/ as canonical example. Adds inbound port Protocols, rewrites import-linter contracts, applies FastAPI best practices (response_model cleanup, write router deps, fastapi CLI entrypoint, tags).
