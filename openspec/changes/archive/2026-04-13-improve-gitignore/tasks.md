## 1. Ignore rules

- [x] 1.1 Add root `.gitignore` with Python, venv, packaging, test/coverage, tool caches, `.env`, IDE/OS noise; ensure `uv.lock` and `.python-version` are not ignored.

## 2. Documentation

- [x] 2.1 Add a short README subsection on Git ignores (env files vs committed `uv.lock`).
