## 1. Makefile

- [x] 1.1 Add a root `Makefile` with `.PHONY` targets: `help` (default), `sync` (`uv sync`), `dev` (`uv run uvicorn ...`), optional `PORT` variable.

## 2. Documentation

- [x] 2.1 Update `README.md` with a "Make" section listing targets and noting that `uv` is required.
