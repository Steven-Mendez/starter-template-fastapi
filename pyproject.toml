[project]
name = "starter-template-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.4",
    "fastapi>=0.135.3",
    "psycopg[binary]>=3.3.3",
    "pydantic>=2.10",
    "pydantic-settings>=2.13.1",
    "sqlmodel>=0.0.38",
    "uvicorn[standard]>=0.44.0",
]

[dependency-groups]
dev = [
    "freezegun>=1.5.5",
    "httpx>=0.28.1",
    "import-linter>=2.1",
    "mypy>=1.18.2",
    "polyfactory>=3.3.0",
    "pre-commit>=4.3.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-clarity>=1.0.1",
    "pytest-cov>=7.1.0",
    "pytest-html>=4.2.0",
    "pytest-randomly>=4.1.0",
    "pytest-sugar>=1.1.1",
    "ruff>=0.14.0",
    "testcontainers[postgresql]>=4.14.2",
]

[tool.fastapi]
entrypoint = "src.main:app"

[tool.pytest.ini_options]
testpaths = ["src"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: fast, no IO",
    "integration: requires docker / external services",
    "e2e: full flow through HTTP layer",
]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--tb=short",
    # pytest-randomly: deterministic-by-default, override with `--randomly-seed=...`
    "-p", "randomly",
]
filterwarnings = ["ignore::DeprecationWarning"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["src"]
branch = true
omit = [
    "src/**/tests/**",
    "src/**/conftest.py",
    "src/conftest.py",
    "src/main.py",
]
data_file = ".coverage"

[tool.coverage.report]
fail_under = 70
show_missing = true
skip_covered = false
precision = 2
exclude_also = [
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "pragma: no cover",
    "\\.\\.\\.\\s*$",
]

[tool.coverage.html]
directory = "reports/coverage"
title = "starter-template-fastapi coverage"
show_contexts = true

[tool.coverage.xml]
output = "reports/coverage.xml"

[tool.coverage.json]
output = "reports/coverage.json"
pretty_print = true

[tool.hatch.build.targets.wheel]
packages = ["src"]
exclude = [
    "**/tests/**",
    "**/conftest.py",
]

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

[tool.mypy]
python_version = "3.14"
files = ["."]
exclude = ["^openspec/", "^\\.cursor/"]
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
no_implicit_optional = true

[tool.importlinter]
root_packages = ["src"]
include_external_packages = true

# Platform must never depend on a feature.
[[tool.importlinter.contracts]]
name = "Platform isolation: no feature imports"
type = "forbidden"
source_modules = ["src.platform"]
forbidden_modules = ["src.features"]

# === Kanban feature contracts ===

# Domain purity: no application, adapters, framework, or other feature imports.
[[tool.importlinter.contracts]]
name = "Kanban domain: pure"
type = "forbidden"
source_modules = ["src.features.kanban.domain"]
forbidden_modules = [
    "src.features.kanban.application",
    "src.features.kanban.adapters",
    "src.features.kanban.composition",
    "src.platform.api",
    "src.platform.persistence",
    "src.platform.config",
    "fastapi",
    "starlette",
    "sqlmodel",
    "sqlalchemy",
    "uvicorn",
    "httpx",
    "alembic",
    "pydantic_settings",
    "psycopg",
    "pydantic",
]

# Application: no adapters/composition/api/framework. Pydantic allowed for DTOs.
[[tool.importlinter.contracts]]
name = "Kanban application: framework-free"
type = "forbidden"
source_modules = ["src.features.kanban.application"]
forbidden_modules = [
    "src.features.kanban.adapters",
    "src.features.kanban.composition",
    "src.platform.api",
    "src.platform.persistence",
    "fastapi",
    "starlette",
    "sqlmodel",
    "sqlalchemy",
    "uvicorn",
    "httpx",
    "alembic",
    "pydantic_settings",
    "psycopg",
]

# Inbound adapters do not reach into outbound adapters or domain directly.
[[tool.importlinter.contracts]]
name = "Kanban inbound adapter: no outbound bypass"
type = "forbidden"
source_modules = ["src.features.kanban.adapters.inbound"]
allow_indirect_imports = true
forbidden_modules = [
    "src.features.kanban.adapters.outbound",
    "src.features.kanban.domain",
    "sqlmodel",
    "sqlalchemy",
    "alembic",
    "uvicorn",
    "psycopg",
]

# Outbound adapters do not import inbound, use cases, or inbound ports.
[[tool.importlinter.contracts]]
name = "Kanban outbound adapter: isolated from inbound and use cases"
type = "forbidden"
source_modules = ["src.features.kanban.adapters.outbound"]
forbidden_modules = [
    "src.features.kanban.adapters.inbound",
    "src.features.kanban.application.use_cases",
    "src.features.kanban.application.ports.inbound",
]

# Inbound dependency direction inside Kanban.
[[tool.importlinter.contracts]]
name = "Kanban inward dependency direction"
type = "layers"
layers = [
    "src.features.kanban.adapters",
    "src.features.kanban.application",
    "src.features.kanban.domain",
]

# Cross-feature isolation: features must not import each other.
# Currently only kanban exists; this contract is the template enforced here
# (extend ``forbidden_modules`` when new features are added).
[[tool.importlinter.contracts]]
name = "Cross-feature isolation: kanban"
type = "forbidden"
source_modules = ["src.features.kanban"]
forbidden_modules = []

# Tests scope: a feature's tests cannot import another feature.
[[tool.importlinter.contracts]]
name = "Kanban tests scope"
type = "forbidden"
source_modules = ["src.features.kanban.tests"]
forbidden_modules = []
